{
  "schemaVersion": 1,
  "id": "timestop",
  "version": "${version}",
  "name": "TimeStop",
  "description": "Server-side Fabric mod: stun players to freeze movement via /stun command + Fabric API bypass for vanilla clients. No custom registry entries — fully vanilla-client compatible.",
  "authors": [
    "TimeStop Contributors"
  ],
  "contact": {},
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "server": [
      "com.timestop.TimeStopMod"
    ]
  },
  "mixins": [
    "timestop.mixins.json",
    "timestop-bypass.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.0",
    "minecraft": "*"
  }
}
